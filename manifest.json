{

	"name": "LocalNotes",
	"description": "Extension for storing simple text notes in the local storage.",
	"version": "1.0",

	"manifest_version": 2,

	"browser_action": {
		"default_title": "LocalNotes",
		"default_icon": "icon.png",
		"default_popup": "popup.html"
	},

	"commands": {
		"_execute_browser_action": {
			"suggested_key": {
				"windows": "Alt+Shift+N",
				"linux": "Alt+Shift+N",
				"mac": "Alt+Shift+N"
			}
		}
	},

	"permissions": [
		"storage"
	]

}
